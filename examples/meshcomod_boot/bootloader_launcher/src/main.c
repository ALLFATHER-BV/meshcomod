// Placeholder app — this project exists only to build the custom bootloader.bin.
// The real apps (recovery in factory, firmware in ota_0) are flashed separately.
#include <stdio.h>
void app_main(void) { printf("meshcomod_boot bootloader_launcher: placeholder app\n"); }
