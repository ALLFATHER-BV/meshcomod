# Spike 1 partition table (16 MB). Two app partitions:
#   factory -> the normal app  ("MAIN")
#   test    -> the GPIO-triggered app ("RECOVERY")  <- BOOTLOADER_APP_TEST target
# We flash the SAME spike binary to both; it reports which one booted.
#
# Name,     Type, SubType,  Offset,    Size,      Flags
nvs,        data, nvs,      0x9000,    0x5000,
otadata,    data, ota,      0xe000,    0x2000,
factory,    app,  factory,  0x10000,   0x200000,
test,       app,  test,     0x210000,  0x200000,
